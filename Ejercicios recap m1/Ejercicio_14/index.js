`use strict`
let tieneunaCuenta = prompt(`Tiene una cuenta?`);
if(tieneunaCuenta === `si`|| tieneunaCuenta === `si`) {
    let conoceContrasena = prompt(`Conoce la constrasena`);
    if(conoceContrasena ===`si`);
    alert(`Acceso concedido`);
}